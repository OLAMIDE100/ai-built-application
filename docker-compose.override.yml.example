# Development override file
# Copy this to docker-compose.override.yml for hot reload during development
# docker-compose will automatically use this file if it exists

version: '3.8'

services:
  backend:
    volumes:
      - ./backend:/app
    # Uncomment for development with auto-reload
    # command: >
    #   sh -c "
    #     python wait-for-db.py &&
    #     python init_db.py &&
    #     uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
    #   "

